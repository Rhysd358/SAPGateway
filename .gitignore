# Server
server/.dart_tool/
server/.packages
server/build/
server/pubspec.lock
server/data/

# Flutter
app/.dart_tool/
app/.packages
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/
app/pubspec.lock
app/.idea/
app/web/
app/android/
app/ios/
app/linux/
app/macos/
app/windows/
app/.metadata

# Local credentials — never commit
CONNECTIONS.md

# SAP extract sample payloads — may contain real personnel data (PII).
# Keep local to each environment; supply per the samples/README.md.
mock-api/samples/*.json

# Build output of deploy/build.ps1 (binary + web build + seeded data/creds)
dist/

# Transport artifacts of deploy/build.ps1 (zip of dist/ + SHA-256)
release/

# Claude Code per-user permissions cache (machine-local, not project config)
.claude/settings.local.json

# Editors
.idea/
.vscode/
*.iml
.DS_Store
